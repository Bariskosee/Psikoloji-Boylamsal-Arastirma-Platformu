{
  "name": "@lpr/ui",
  "version": "0.0.0",
  "private": true,
  "description": "Shared UI primitives and design tokens for both frontends",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'consumed as source via transpilePackages'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'no tests yet'"
  },
  "devDependencies": {
    "@lpr/config": "workspace:*",
    "@types/react": "^19.0.2",
    "typescript": "^5.7.2"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  }
}
