{
  "name": "@lpr/config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript and Vitest configuration",
  "files": [
    "tsconfig.node.json",
    "tsconfig.next.json"
  ],
  "scripts": {
    "build": "echo 'no build step (config only)'",
    "typecheck": "echo 'no typecheck (config only)'",
    "test": "echo 'no tests (config only)'"
  }
}
